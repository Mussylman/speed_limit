# 🚦 Sergek Frontend - Система Видеонаблюдения

![React](https://img.shields.io/badge/React-19.2.0-61DAFB?style=for-the-badge&logo=react)
![TypeScript](https://img.shields.io/badge/TypeScript-5.9.3-3178C6?style=for-the-badge&logo=typescript)
![Vite](https://img.shields.io/badge/Vite-7.2.5-646CFF?style=for-the-badge&logo=vite)
![TailwindCSS](https://img.shields.io/badge/Tailwind-4.1.18-06B6D4?style=for-the-badge&logo=tailwindcss)

Современная веб-система видеонаблюдения для города Шымкент с функциями распознавания номерных знаков, мониторинга нарушений ПДД и управления камерами в реальном времени.

## 📋 Содержание

- [Особенности](#-особенности)
- [Технологический стек](#-технологический-стек)
- [Требования к системе](#-требования-к-системе)
- [Установка](#-установка)
- [Запуск проекта](#-запуск-проекта)
- [Структура проекта](#-структура-проекта)
- [Функциональность](#-функциональность)
- [API интеграция](#-api-интеграция)
- [Многоязычность](#-многоязычность)
- [Тестирование](#-тестирование)
- [Сборка для продакшена](#-сборка-для-продакшена)
- [Развертывание](#-развертывание)
- [Вклад в проект](#-вклад-в-проект)
- [Лицензия](#-лицензия)

## ✨ Особенности

### 🎯 Основные функции
- **Панель управления** - Централизованный мониторинг всей системы
- **Управление камерами** - Добавление, редактирование и удаление камер
- **Живое видео** - Просмотр RTSP/HLS потоков в реальном времени
- **Умные камеры** - Распознавание номерных знаков с ИИ
- **Карта города** - Интерактивная карта с расположением камер
- **Нарушения ПДД** - Автоматическое обнаружение и управление нарушениями
- **Поиск транспорта** - Отслеживание маршрутов по номерным знакам

### 🎨 Дизайн и UX
- **Адаптивный дизайн** - Работает на всех устройствах
- **Темная/светлая тема** - Современный белый интерфейс с тенями
- **Анимации** - Плавные переходы с Framer Motion
- **Интуитивная навигация** - Простой и понятный интерфейс

### 🌐 Многоязычность
- **Русский** (по умолчанию)
- **Казахский** 
- **Английский**
- Автоматическое сохранение выбранного языка

## 🛠 Технологический стек

### Frontend Framework
- **React 19.2.0** - Современная библиотека для создания пользовательских интерфейсов
- **TypeScript 5.9.3** - Типизированный JavaScript для надежности кода
- **Vite 7.2.5** - Быстрый сборщик и dev-сервер

### Стилизация и UI
- **TailwindCSS 4.1.18** - Utility-first CSS фреймворк
- **Framer Motion 12.26.2** - Библиотека анимаций
- **Lucide React 0.562.0** - Современные SVG иконки

### Состояние и маршрутизация
- **Zustand 5.0.10** - Легкое управление состоянием
- **React Router DOM 7.12.0** - Клиентская маршрутизация

### Карты и геолокация
- **Leaflet 1.9.4** - Интерактивные карты
- **React Leaflet 5.0.0** - React компоненты для Leaflet

### Видео и стриминг
- **HLS.js 1.6.15** - HTTP Live Streaming для браузеров
- Поддержка RTSP через HLS конвертацию

### Интернационализация
- **i18next 25.7.4** - Система переводов
- **react-i18next 16.5.2** - React интеграция для i18n

### HTTP клиент
- **Axios 1.13.2** - Promise-based HTTP клиент

### Инструменты разработки
- **ESLint 9.39.1** - Линтер для JavaScript/TypeScript
- **Prettier 3.7.4** - Форматтер кода
- **Vitest 4.0.17** - Тестовый фреймворк
- **Testing Library** - Утилиты для тестирования React

## 💻 Требования к системе

### Минимальные требования
- **Node.js** >= 18.0.0
- **npm** >= 9.0.0 или **yarn** >= 1.22.0
- **Git** >= 2.0.0

### Рекомендуемые требования
- **Node.js** >= 20.0.0
- **npm** >= 10.0.0
- **8 GB RAM** для комфортной разработки
- **SSD** для быстрой работы

### Браузеры
- **Chrome** >= 90
- **Firefox** >= 88
- **Safari** >= 14
- **Edge** >= 90

## 🚀 Установка

### 1. Клонирование репозитория
```bash
git clone https://github.com/your-username/sergek-frontend.git
cd sergek-frontend
```

### 2. Установка зависимостей
```bash
# Используя npm
npm install

# Или используя yarn
yarn install
```

### 3. Настройка переменных окружения
Создайте файл `.env.local` в корне проекта:
```env
# API Configuration
VITE_API_BASE_URL=http://localhost:8000/api
VITE_WS_URL=ws://localhost:8000/ws

# Map Configuration
VITE_MAP_CENTER_LAT=42.3417
VITE_MAP_CENTER_LNG=69.5901
VITE_MAP_DEFAULT_ZOOM=13

# Video Streaming
VITE_HLS_TIMEOUT=30000
VITE_RTSP_TIMEOUT=10000

# Development
VITE_DEV_MODE=true
VITE_LOG_LEVEL=debug
```

## 🏃‍♂️ Запуск проекта

### Режим разработки
```bash
npm run dev
```
Приложение будет доступно по адресу: `http://localhost:5173`

### Сборка для продакшена
```bash
npm run build
```

### Предварительный просмотр сборки
```bash
npm run preview
```

### Запуск тестов
```bash
# Однократный запуск тестов
npm run test

# Режим наблюдения
npm run test:watch
```

### Линтинг кода
```bash
npm run lint
```

## 📁 Структура проекта

```
sergek-frontend/
├── public/                     # Статические файлы
│   └── vite.svg               # Иконка приложения
├── src/                       # Исходный код
│   ├── components/            # React компоненты
│   │   ├── camera/           # Компоненты камер
│   │   │   ├── CameraCard.tsx
│   │   │   ├── CameraGrid.tsx
│   │   │   ├── VideoPlayer.tsx
│   │   │   └── index.ts
│   │   ├── common/           # Общие компоненты
│   │   │   ├── animations/   # Анимации
│   │   │   ├── skeletons/    # Скелетоны загрузки
│   │   │   ├── states/       # Состояния (загрузка, ошибка)
│   │   │   ├── LanguageSwitcher.tsx
│   │   │   └── index.ts
│   │   ├── layout/           # Компоненты макета
│   │   │   ├── Header.tsx
│   │   │   ├── Layout.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   └── index.ts
│   │   ├── map/              # Компоненты карты
│   │   │   ├── CameraMarker.tsx
│   │   │   ├── MapEditControls.tsx
│   │   │   ├── MapView.tsx
│   │   │   ├── RouteVisualizer.tsx
│   │   │   └── index.ts
│   │   ├── plate/            # Компоненты номерных знаков
│   │   │   ├── PlateDetectionList.tsx
│   │   │   ├── PlateDisplay.tsx
│   │   │   ├── PlateSearch.tsx
│   │   │   └── index.ts
│   │   ├── vehicle/          # Компоненты транспорта
│   │   │   ├── VehicleRoutePanel.tsx
│   │   │   └── index.ts
│   │   └── violation/        # Компоненты нарушений
│   │       ├── ViolationCard.tsx
│   │       ├── ViolationFilters.tsx
│   │       └── index.ts
│   ├── i18n/                 # Интернационализация
│   │   ├── locales/          # Файлы переводов
│   │   │   ├── en.json       # Английский
│   │   │   ├── kk.json       # Казахский
│   │   │   └── ru.json       # Русский
│   │   └── index.ts          # Конфигурация i18n
│   ├── pages/                # Страницы приложения
│   │   ├── AdminCamerasPage.tsx
│   │   ├── AdminPage.tsx
│   │   ├── CameraDetailPage.tsx
│   │   ├── CameraFormPage.tsx
│   │   ├── CamerasPage.tsx
│   │   ├── Dashboard.tsx
│   │   ├── MapPage.tsx
│   │   ├── SmartCamerasPage.tsx
│   │   ├── VehicleDetailPage.tsx
│   │   ├── ViolationDetailPage.tsx
│   │   ├── ViolationsPage.tsx
│   │   └── index.ts
│   ├── router/               # Маршрутизация
│   │   └── index.tsx
│   ├── services/             # API сервисы
│   │   ├── api/
│   │   │   ├── cameraService.ts
│   │   │   ├── client.ts
│   │   │   ├── vehicleService.ts
│   │   │   ├── violationService.ts
│   │   │   └── index.ts
│   │   └── index.ts
│   ├── stores/               # Zustand хранилища
│   │   ├── cameraStore.ts
│   │   ├── mapStore.ts
│   │   ├── vehicleStore.ts
│   │   ├── violationStore.ts
│   │   └── index.ts
│   ├── styles/               # Глобальные стили
│   │   ├── components.css
│   │   └── design-tokens.css
│   ├── test/                 # Настройки тестов
│   │   └── setup.ts
│   ├── types/                # TypeScript типы
│   │   └── index.ts
│   ├── utils/                # Утилиты
│   │   └── cn.ts
│   ├── App.css              # Стили приложения
│   ├── App.tsx              # Главный компонент
│   ├── index.css            # Глобальные стили
│   └── main.tsx             # Точка входа
├── .gitignore               # Git игнорируемые файлы
├── .prettierignore          # Prettier игнорируемые файлы
├── .prettierrc              # Конфигурация Prettier
├── eslint.config.js         # Конфигурация ESLint
├── index.html               # HTML шаблон
├── package.json             # Зависимости и скрипты
├── tailwind.config.js       # Конфигурация Tailwind
├── tsconfig.json            # Конфигурация TypeScript
├── vite.config.ts           # Конфигурация Vite
└── vitest.config.ts         # Конфигурация Vitest
```

## 🎯 Функциональность

### 📊 Панель управления (Dashboard)
- Обзор системы в реальном времени
- Статистика камер и нарушений
- Быстрый доступ к основным функциям
- Мониторинг состояния системы

### 📹 Управление камерами
- **Просмотр всех камер** - Сетка с информацией о статусе
- **Добавление камер** - Форма с валидацией и тестом подключения
- **Редактирование** - Изменение параметров существующих камер
- **Удаление** - Безопасное удаление с подтверждением
- **Живое видео** - RTSP/HLS стриминг в реальном времени

### 🧠 Умные камеры
- **Распознавание номеров** - ИИ анализ в реальном времени
- **История детекций** - Журнал всех распознанных номеров
- **Статистика точности** - Метрики работы алгоритмов
- **Фильтрация** - Поиск по номерам и времени

### 🗺 Интерактивная карта
- **Расположение камер** - Визуализация на карте Шымкента
- **Редактирование позиций** - Перетаскивание маркеров
- **Добавление камер** - Клик по карте для размещения
- **Маршруты транспорта** - Отображение путей движения

### 🚨 Нарушения ПДД
- **Автоматическое обнаружение** - ИИ анализ нарушений
- **Классификация** - Типы нарушений (скорость, красный свет, и т.д.)
- **Управление статусами** - Подтверждение/отклонение нарушений
- **Доказательства** - Фото и видео материалы

### 🚗 Отслеживание транспорта
- **Поиск по номерам** - Быстрый поиск транспортных средств
- **История движения** - Маршруты и временные метки
- **Информация о владельце** - Данные из государственных баз
- **Статистика** - Частота проездов и нарушений

## 🔌 API интеграция

### Базовая конфигурация
```typescript
// src/services/api/client.ts
const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8000/api'

const apiClient = axios.create({
  baseURL: API_BASE_URL,
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json',
  },
})
```

### Основные эндпоинты

#### Камеры
```typescript
GET    /api/cameras              # Получить все камеры
POST   /api/cameras              # Создать камеру
GET    /api/cameras/{id}         # Получить камеру по ID
PATCH  /api/cameras/{id}         # Обновить камеру
DELETE /api/cameras/{id}         # Удалить камеру
POST   /api/cameras/test-connection  # Тест RTSP подключения
```

#### Нарушения
```typescript
GET    /api/violations           # Получить нарушения
POST   /api/violations           # Создать нарушение
GET    /api/violations/{id}      # Получить нарушение по ID
PATCH  /api/violations/{id}/status   # Обновить статус
```

#### Транспорт
```typescript
GET    /api/vehicles/{plate}     # Информация о транспорте
GET    /api/vehicles/{plate}/route   # Маршрут движения
GET    /api/detections           # История распознаваний
```

### Обработка ошибок
```typescript
// Автоматическая обработка ошибок API
apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      // Перенаправление на авторизацию
    }
    return Promise.reject(error)
  }
)
```

## 🌐 Многоязычность

### Поддерживаемые языки
- **Русский (ru)** - Язык по умолчанию
- **Казахский (kk)** - Государственный язык Казахстана  
- **Английский (en)** - Международный язык

### Использование переводов
```typescript
import { useTranslation } from 'react-i18next'

function MyComponent() {
  const { t, i18n } = useTranslation()
  
  return (
    <div>
      <h1>{t('pages.dashboard.title')}</h1>
      <button onClick={() => i18n.changeLanguage('kk')}>
        Қазақша
      </button>
    </div>
  )
}
```

### Добавление новых переводов
1. Добавьте ключ в `src/i18n/locales/ru.json`
2. Переведите на другие языки в соответствующих файлах
3. Используйте `t('your.new.key')` в компонентах

### Структура переводов
```json
{
  "nav": {
    "dashboard": "Панель управления",
    "cameras": "Камеры"
  },
  "pages": {
    "dashboard": {
      "title": "Панель управления",
      "description": "Обзор системы"
    }
  }
}
```

## 🧪 Тестирование

### Запуск тестов
```bash
# Все тесты
npm run test

# Тесты в режиме наблюдения
npm run test:watch

# Покрытие кода
npm run test -- --coverage
```

### Типы тестов

#### Unit тесты
```typescript
// src/stores/__tests__/cameraStore.test.ts
import { renderHook, act } from '@testing-library/react'
import { useCameraStore } from '../cameraStore'

test('should add camera', () => {
  const { result } = renderHook(() => useCameraStore())
  
  act(() => {
    result.current.addCamera(mockCamera)
  })
  
  expect(result.current.cameras).toHaveLength(1)
})
```

#### Component тесты
```typescript
// src/components/__tests__/CameraCard.test.tsx
import { render, screen } from '@testing-library/react'
import { CameraCard } from '../CameraCard'

test('renders camera information', () => {
  render(<CameraCard camera={mockCamera} />)
  
  expect(screen.getByText('Test Camera')).toBeInTheDocument()
  expect(screen.getByText('Online')).toBeInTheDocument()
})
```

#### Property-based тесты
```typescript
// src/stores/cameraStore.property.test.ts
import fc from 'fast-check'

test('camera store properties', () => {
  fc.assert(fc.property(
    fc.array(cameraArbitrary),
    (cameras) => {
      // Тест инвариантов хранилища
    }
  ))
})
```

## 🏗 Сборка для продакшена

### Создание production сборки
```bash
npm run build
```

### Оптимизация сборки
```typescript
// vite.config.ts
export default defineConfig({
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['react', 'react-dom'],
          router: ['react-router-dom'],
          maps: ['leaflet', 'react-leaflet'],
          video: ['hls.js']
        }
      }
    },
    chunkSizeWarningLimit: 1000
  }
})
```

### Анализ размера бандла
```bash
npm run build -- --analyze
```

## 🚀 Развертывание

### Docker развертывание
```dockerfile
# Dockerfile
FROM node:20-alpine as builder

WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production

COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/nginx.conf

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

### Nginx конфигурация
```nginx
# nginx.conf
server {
    listen 80;
    server_name localhost;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
    
    location /api {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### Переменные окружения для продакшена
```env
VITE_API_BASE_URL=https://api.sergek.kz/api
VITE_WS_URL=wss://api.sergek.kz/ws
VITE_MAP_CENTER_LAT=42.3417
VITE_MAP_CENTER_LNG=69.5901
VITE_DEV_MODE=false
VITE_LOG_LEVEL=error
```

## 🤝 Вклад в проект

### Процесс разработки
1. **Fork** репозитория
2. Создайте **feature branch** (`git checkout -b feature/amazing-feature`)
3. **Commit** изменения (`git commit -m 'Add amazing feature'`)
4. **Push** в branch (`git push origin feature/amazing-feature`)
5. Откройте **Pull Request**

### Стандарты кода
- Используйте **TypeScript** для всего кода
- Следуйте **ESLint** правилам
- Форматируйте код с **Prettier**
- Покрывайте код **тестами**
- Документируйте **API** изменения

### Commit сообщения
```
feat: добавить распознавание номеров
fix: исправить ошибку загрузки видео
docs: обновить README
style: форматирование кода
refactor: рефакторинг компонента камеры
test: добавить тесты для store
chore: обновить зависимости
```

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 📞 Поддержка

### Контакты
- **Email**: support@sergek.kz
- **Telegram**: @sergek_support
- **GitHub Issues**: [Создать issue](https://github.com/your-username/sergek-frontend/issues)

### Документация
- **API Docs**: https://api.sergek.kz/docs
- **User Guide**: https://docs.sergek.kz
- **Developer Guide**: https://dev.sergek.kz

---

**Разработано с ❤️ для города Шымкент**

*Система Sergek - современное решение для безопасности дорожного движения*